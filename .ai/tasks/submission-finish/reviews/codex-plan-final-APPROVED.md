1. Prior P2: “No Open Graph or Twitter metadata exists; a social card could ship but never
   appear.” **Addressed.** The plan now requires text metadata, builds and verifies `og.png`, and
   permits absolute image references only after the public URL is verified.

No new actionable issues introduced.

APPROVED
